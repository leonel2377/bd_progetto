# Configurazione Database
# Per PostgreSQL (produzione)
DATABASE_URL=postgresql://username:password@localhost:5432/flight_booking

# Per SQLite (sviluppo locale) - decommenta la riga sotto se vuoi usare SQLite
# DATABASE_URL=sqlite:///flight_booking.db

# Configurazione Mail (opzionale)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_password

# Configurazione Flask
FLASK_ENV=development
FLASK_DEBUG=true